<p id="notice"><%= notice %></p>

<h1>Address Book</h1>
<h2>Associate</h2>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Occupation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @people.each do |person| %>
      <tr>
        <td class="userinfo"><%= person.name %></td>
        <td class="userinfo"><%= person.address %></td>
        <td class="userinfo"><%= person.phone %></td>
        <td class="userinfo"><%= person.occupation.title %></td>
        <td class="crudbuttons"><%= link_to 'Show', person %></td>
        <td class="crudbuttons"><%= link_to 'Edit', edit_person_path(person) %></td>
        <td class="crudbuttons"><%= link_to 'Destroy', person, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Person', new_person_path %>
